1) Doulos quote: imp of non-virtual method is chosen based on the variable/handle that is used to reference it.

Q1) Why can a handle not point to it's parent type?  I guess b/c it could include other properties that are then lost.
